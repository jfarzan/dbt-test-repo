sources:
  - name: jshop
    description: jaffle shop tables
    database: raw
    schema: jshop
    tables:
      - name: customers
        description: customers raw table
        columns:
        - name: id
          description: unique customer id
          data_tests:
            - unique
            - not_null
      - name: orders
        description: orders raw table
        columns:
          - name: id
            description: unique order id
            data_tests:
              - unique
              - not_null
        config:
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 20
              period: day
          loaded_at_field: _ETL_LOADED_AT